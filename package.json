{
	"name": "scorekinole",
	"private": true,
	"version": "2.1.7",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"sync": "npx cap sync",
		"build:apk": "npm run build && npx cap sync android && cd android && .\\gradlew.bat app:assembleRelease && cd .. && node rename-apk.js",
		"release": "node scripts/create-release.js",
		"deploy": "npm run build && firebase deploy",
		"deploy:rules": "firebase deploy --only firestore:rules",
		"deploy:functions": "firebase deploy --only functions",
		"functions:log": "firebase functions:log"
	},
	"devDependencies": {
		"@capacitor/assets": "^3.0.5",
		"@sveltejs/adapter-static": "^3.0.0",
		"@sveltejs/kit": "^2.0.0",
		"@sveltejs/vite-plugin-svelte": "^3.0.0",
		"svelte": "^4.2.0",
		"svelte-check": "^3.6.0",
		"typescript": "^5.3.0",
		"vite": "^5.0.0"
	},
	"dependencies": {
		"@capacitor-firebase/authentication": "^6.0.0",
		"@capacitor/android": "^6.0.0",
		"@capacitor/cli": "^6.0.0",
		"@capacitor/core": "^6.0.0",
		"firebase": "^10.13.0"
	}
}
