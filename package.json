{
  "version": "1.0.1",
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  },
  "scripts": {
    "build:apk": "node sync-version.js && npx cap sync android && cd android && gradlew app:assembleRelease && node ../rename-apk.js"
  }
}
