# CLAUDE.md

Follow **[AGENTS.md](AGENTS.md)** as the primary repository instructions — project overview, architecture, styling, i18n, Firebase, testing, and critical rules all live there.

## Claude-specific

- Don't mention yourself in commits — **NEVER** add `Co-Authored-By` lines.
- **NEVER** create commits or push to remote without an explicit request from the user.
- **NEVER** run `npm run dev` / `npm run build` / `svelte-check` — the user runs these manually.
