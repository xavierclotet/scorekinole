// Prerender so Googlebot gets real HTML instead of the empty 200.html SPA shell.
// Leaderboard data hydrates client-side from the precomputed /playerStats aggregates.
export const prerender = true;
